# 📋 01. MVP Specification & Tech Stack
> **Проект:** IndustrialPROFI (Modern Monolith)
> **Версия:** 2.0 (React/Solid Rewrites)
> **Статус:** Определение MVP

## 1. Суть продукта (Core Value)

IndustrialPROFI — это B2B-платформа для управления квалификацией промышленного персонала. MVP состоит из двух строго изолированных модулей:

### 🅰️ Модуль: Матрица компетенций (Skill Matrix)
*   **Для кого:** HR, Технические руководители.
*   **Функция:** Визуализация профессионального развития сотрудников через интерактивные карты (Roadmaps), аналогичные *roadmap.sh*.
*   **Ценность:** Понимание, кто и что умеет, автоматическое построение планов развития.

### 🅱️ Модуль: Трекинг допусков (Permits)
*   **Для кого:** Служба Охраны Труда (ОТ), Главные инженеры.
*   **Функция:** Учет сроков действия обязательных сертификатов (НАКС, Электробезопасность, Промбез).
*   **Ценность:** Избежание штрафов за просроченные допуски сотрудников.

---

## 2. Технический стек (Strict Constraints)

Мы используем архитектуру **Modern Monolith** на базе Rails 8. Отказ от микросервисов и сложной инфраструктуры в пользу производительности и простоты деплоя.

### Backend (Ruby on Rails 8)
*   **Language:** Ruby 3.4.1
*   **Framework:** Rails 8.0.1
*   **Database:** SQLite (Production-ready конфигурация с WAL).
*   **Background Jobs:** `Solid Queue` (хранение задач в БД, без Redis).
*   **Caching:** `Solid Cache` (кэширование в БД, без Redis).
*   **WebSockets:** `Solid Cable` (события в БД, без Redis).
*   **Serialization:** `Blueprinter` или `Jbuilder` (оптимизировано под JSON props для Inertia).

### Frontend (React 19 + Inertia)
*   **Framework:** React 19.
*   **Integration:** Inertia.js (Rails Adapter). Позволяет писать SPA, используя роутинг и контроллеры Rails.
*   **Build Tool:** Vite 7 (via `vite_rails` gem).
*   **Language:** TypeScript (Strict Mode).
*   **Styling:** Tailwind CSS 3.4.
*   **Graph Engine:** **React Flow** (визуализация дорожных карт, замена Cytoscape).
*   **State Management:** React Context / Hooks (URL search params для шаринга состояния UI).

### Infrastructure
*   **Deploy:** Kamal 2.
*   **Container:** Docker (Multi-stage build).

---

## 3. Scope Cut (Исключено из MVP)

Ради скорости разработки (Time-to-Market) следующие функции **строго исключены**:

1.  ❌ **Геймификация:** Никаких ачивок, баллов, лидербордов.
2.  ❌ **Загрузка файлов:** Допуски заводятся только мета-данными (номер, дата). Скан сертификата не храним.
3.  ❌ **Сложный RBAC:** Только 2 роли: `Admin` (видит всё) и `User` (видит только себя и свой отдел).
4.  ❌ **Редактор карт в UI:** Дорожные карты создаются разработчиками через YAML-файлы и импортируются скриптом.
5.  ❌ **Мобильное приложение:** Только адаптивная веб-версия.
6.  ❌ **SSR (Server Side Rendering):** Используем стандартный Inertia CSR (Client Side Rendering) для простоты.
7.  ❌ **Комментарии/Чат:** Обсуждение задач происходит вне платформы.

---

## 4. Ключевые сущности (MVP)

1.  **Roadmap (Карта):** Граф навыков для конкретной профессии (напр. "Сварщик").
2.  **Skill (Навык/Узел):** Единица компетенции. Может быть "Хард-скиллом" или "Допуском".
3.  **UserProgress:** Связь Сотрудник <-> Навык. Хранит статус (`todo`, `in_progress`, `done`) и оценку.
4.  **Resource:** Ссылка на материал для изучения (Статья, Видео), привязанная к Навыку.

# 🚀 MVP+ Features (Post-Launch)

> **Статус:** Roadmap для v2  
> **Время реализации:** После первых продаж (2-6 месяцев после MVP)

---

## 🎯 Философия MVP+

**MVP** (сейчас) = Минимум функционала для первых продаж  
**MVP+** (потом) = Функции на основе реального фидбека клиентов

**Правило:** Не добавляем фичу в MVP+, пока минимум 3 клиента не попросят её.

---

## 📊 Критерии Готовности к MVP+

Переходим к MVP+ только после:
- ✅ **5+ платящих компаний** используют MVP
- ✅ **60 дней uptime** без критичных багов
- ✅ **Собрано 20+ запросов** от клиентов на новые фичи
- ✅ **LTV/CAC \u003e 3** (экономика здоровая)

---

## 🗂️ Категории MVP+ Фич

### 1️⃣ Интеграция с 1С:ЗУП
### 2️⃣ Расширенная Аналитика
### 3️⃣ Улучшения UX
### 4️⃣ Enterprise Features
### 5️⃣ On-Premise Deployment

---

## 1️⃣ Интеграция с 1С:ЗУП

### Проблема Клиента:
\u003e "У нас все сотрудники уже есть в 1С. Не хочу дублировать данные вручную."

### Решение:
**Двусторонняя синхронизация** сотрудников и допусков с 1С:Зарплата и Управление Персоналом.

---

### Фичи:

#### A. Импорт Сотрудников из 1С
**User Story:**
1. Manager открывает `/integrations/1c`
2. Настраивает подключение (URL API 1С, логин/пароль)
3. Нажимает "Синхронизировать сотрудников"
4. Backend через OData/REST API 1С получает:
   - ФИО
   - Табельный номер
   - Должность
   - Подразделение
   - Дата приема
5. Создаются Users с `external_1c_id` (связь)

**Логика:**
```ruby
# app/services/one_c/sync_service.rb
class OneC::SyncService
  def sync_employees(organization)
    api = OneC::ApiClient.new(organization.onec_credentials)
    
    employees = api.fetch_employees
    
    employees.each do |emp_data|
      user = organization.users.find_or_initialize_by(
        external_1c_id: emp_data['Ref_Key']
      )
      
      user.assign_attributes(
        full_name: emp_data['Description'],
        department: emp_data['Department'],
        metadata: {
          personnel_number: emp_data['Code'],
          hire_date: emp_data['DateOfHire']
        }
      )
      
      user.save!
    end
  end
end
```

**UI:**
```tsx
\u003cCard\u003e
  \u003ch3\u003eИнтеграция с 1С:ЗУП\u003c/h3\u003e
  
  \u003cform onSubmit={handleConnect}\u003e
    \u003cInput label="URL API 1С" name="api_url" /\u003e
    \u003cInput label="Логин" name="username" /\u003e
    \u003cInput label="Пароль" type="password" name="password" /\u003e
    
    \u003cButton type="submit"\u003eПодключить\u003c/Button\u003e
  \u003c/form\u003e
  
  {connected \u0026\u0026 (
    \u003c\u003e
      \u003cdiv className="text-green-600"\u003e✓ Подключено\u003c/div\u003e
      \u003cButton onClick={handleSync}\u003e
        Синхронизировать сотрудников ({employeesCount})
      \u003c/Button\u003e
      
      \u003cdiv className="text-sm text-gray-500"\u003e
        Последняя синхронизация: {lastSyncAt}
      \u003c/div\u003e
    \u003c/\u003e
  )}
\u003c/Card\u003e
```

**Время разработки:** 2 недели

---

#### B. Автосинхронизация (Webhook)
**Проблема:** Вручную синхронизировать каждый раз неудобно.

**Решение:** Webhook от 1С при изменении данных сотрудника.

**Логика:**
```ruby
# config/routes.rb
post '/webhooks/1c/employee_updated', to: 'webhooks/one_c#employee_updated'

# app/controllers/webhooks/one_c_controller.rb
class Webhooks::OneCController \u003c ApplicationController
  skip_before_action :verify_authenticity_token  # Webhook от внешней системы
  before_action :verify_1c_signature
  
  def employee_updated
    external_id = params['Ref_Key']
    organization = Organization.find_by(onec_credentials: ...)
    
    OneC::SyncEmployeeJob.perform_later(organization.id, external_id)
    
    head :ok
  end
  
  private
  
  def verify_1c_signature
    # Проверка подписи запроса от 1С
  end
end
```

**Время разработки:** 1 неделя

---

#### C. Экспорт Допусков в 1С
**User Story:** Допуски оформленные в IndustrialPROFI автоматически попадают в 1С.

**Логика:**
```ruby
# app/models/user_progress.rb
class UserProgress \u003c ApplicationRecord
  after_update :sync_to_1c, if: :permit_completed?
  
  private
  
  def sync_to_1c
    return unless user.organization.onec_integration_enabled?
    
    OneC::ExportPermitJob.perform_later(self.id)
  end
  
  def permit_completed?
    saved_change_to_status? \u0026\u0026 status == 'completed' \u0026\u0026 skill.permit?
  end
end

# app/jobs/one_c/export_permit_job.rb
class OneC::ExportPermitJob \u003c ApplicationJob
  def perform(progress_id)
    progress = UserProgress.find(progress_id)
    api = OneC::ApiClient.new(progress.user.organization.onec_credentials)
    
    api.create_or_update_permit(
      employee_ref: progress.user.external_1c_id,
      permit_data: {
        title: progress.skill.title,
        certificate_number: progress.certificate_number,
        issued_at: progress.issued_at,
        expires_at: progress.expires_at
      }
    )
  end
end
```

**Время разработки:** 2 недели

---

### Итого по 1С интеграции:
**Время:** 5 недель  
**Ценность для клиента:** Устраняет дублирование данных, критично для крупных заводов  
**Pricing:** Доступно только на тарифе **Корпоративный** (120,000₽/мес)

---

## 2️⃣ Расширенная Аналитика

### A. Отчеты для Ростехнадзора
**Проблема:** Во время проверки Ростехнадзора нужно предоставить списки сотрудников с допусками.

**Решение:** Генерация готовых отчетов в формате Excel/PDF.

**Фичи:**
- Отчет "Сотрудники с допусками" (дата, ФИО, номер удостоверения, срок действия)
- Фильтр по отделу, должности, типу допуска
- Экспорт в Excel с брендированием компании

**UI:**
```tsx
\u003cCard\u003e
  \u003ch3\u003eОтчет для Ростехнадзора\u003c/h3\u003e
  
  \u003cSelect label="Отдел" options={departments} /\u003e
  \u003cSelect label="Тип допуска" options={permitTypes} /\u003e
  \u003cDateRangePicker label="Период" /\u003e
  
  \u003cButton onClick={generateExcel}\u003e
    Скачать Excel
  \u003c/Button\u003e
  
  \u003cButton onClick={generatePdf}\u003e
    Скачать PDF (с подписью)
  \u003c/Button\u003e
\u003c/Card\u003e
```

**Время разработки:** 1 неделя

---

### B. Dashboard для Owner (Высокоуровневая Аналитика)
**Фичи:**
- % сотрудников с актуальными допусками (по отделам)
- Тренд compliance за последние 6 месяцев
- Топ-5 допусков, которые чаще всего просрочены
- Прогноз затрат на обучение (сколько сотрудников нужно обучить в ближайшие 3 месяца)

**Время разработки:** 2 недели

---

## 3️⃣ Улучшения UX

### A. Drag-and-Drop Узлов в Редакторе Roadmap
**Проблема:** Auto-layout не всегда расставляет узлы оптимально.

**Решение:** Возможность перетаскивать узлы вручную.

**Логика:**
```tsx
const onNodeDragStop = useCallback((event, node) =\u003e {
  router.patch(`/organizations/roadmaps/${roadmapId}/skills/${node.id}`, {
    position_x: node.position.x,
    position_y: node.position.y
  });
}, []);

\u003cReactFlow 
  nodes={nodes}
  onNodeDragStop={onNodeDragStop}
  nodesDraggable={true}  // Включаем drag
/\u003e
```

**Время разработки:** 3 дня

---

### B. Подтверждение Навыков Менеджером
**Проблема:** Сотрудники могут ошибочно/намеренно отмечать навыки как изученные.

**Решение:** Двухэтапный процесс:
1. Сотрудник отмечает `status = 'pending_review'`
2. Менеджер подтверждает → `status = 'completed'`

**UI для менеджера:**
```tsx
\u003cCard\u003e
  \u003ch3\u003eОжидают подтверждения\u003c/h3\u003e
  
  {pendingProgress.map(progress =\u003e (
    \u003cdiv key={progress.id}\u003e
      {progress.user.full_name} → {progress.skill.title}
      
      \u003cButton onClick={() =\u003e approve(progress.id)}\u003e
        ✓ Подтвердить
      \u003c/Button\u003e
      
      \u003cButton variant="outline" onClick={() =\u003e reject(progress.id)}\u003e
        ✗ Отклонить
      \u003c/Button\u003e
    \u003c/div\u003e
  ))}
\u003c/Card\u003e
```

**Время разработки:** 1 неделя

---

### C. Загрузка Скан-Копий Сертификатов
**Проблема:** Для аудита нужны скан-копии удостоверений.

**Решение:** Active Storage для загрузки файлов.

**Логика:**
```ruby
# app/models/user_progress.rb
class UserProgress \u003c ApplicationRecord
  has_one_attached :certificate_scan
end

# app/controllers/organizations/progress_controller.rb
def update
  progress.certificate_scan.attach(params[:certificate_scan])
  # ...
end
```

**UI:**
```tsx
\u003cFileUpload 
  label="Скан удостоверения (PDF/JPG)"
  accept=".pdf,.jpg,.jpeg,.png"
  maxSize={10 * 1024 * 1024}  // 10MB
  onChange={handleFileUpload}
/\u003e

{progress.certificate_scan_url \u0026\u0026 (
  \u003ca href={progress.certificate_scan_url} target="_blank"\u003e
    📄 Просмотреть скан
  \u003c/a\u003e
)}
```

**Время разработки:** 4 дня

---

## 4️⃣ Enterprise Features

### A. SSO (Single Sign-On)
**Проблема:** Крупные заводы используют корпоративную AD/LDAP.

**Решение:** Интеграция с SAML/OAuth.

**Время разработки:** 2 недели  
**Pricing:** Только тариф Корпоративный

---

### B. Audit Trail (Расширенный)
**Проблема:** Для госконтрактов нужен immutable audit log.

**Решение:** Миграция с `paper_trail` на `pgaudit`.

**Время разработки:** 1 неделя  
**Pricing:** Только тариф Корпоративный

---

## 5️⃣ On-Premise Deployment

### Проблема:
Заводы с режимными объектами не могут использовать SaaS (требование безопасности).

### Решение:
**On-Premise пакет:**
- Docker Compose setup для развертывания на сервере клиента
- Документация для IT-отдела клиента
- Поддержка установки (2 дня работы инженера)

**Pricing:**
- Единоразово: 800,000₽
- Поддержка: 20,000₽/месяц

**Время разработки:** 3 недели

---

## 📅 Рекомендуемый Roadmap MVP+

### Квартал 1 (после MVP):
1. Drag-and-drop узлов (3 дня)
2. Отчеты для Ростехнадзора (1 неделя)
3. Подтверждение навыков менеджером (1 неделя)

### Квартал 2:
1. Интеграция с 1С (5 недель) — если 3+ клиента запросили
2. Загрузка скан-копий (4 дня)

### Квартал 3:
1. Dashboard для Owner (2 недели)
2. SSO (2 недели) — если есть enterprise клиенты

### Квартал 4:
1. On-Premise пакет (3 недели) — если есть заказ

---

## 🚫 Что НЕ Включено в MVP+

### Отложено на v3:
- Мобильное приложение (iOS/Android)
- Английская локализация
- API для интеграций
- Геймификация (баджи, лидерборды)
- Video tutorials внутри платформы
- AI-powered рекомендации roadmaps

**Причина:** Нет запросов от клиентов. Фокусируемся на core value.

---

## 💰 Монетизация MVP+ Фич

### Модель:
- **Базовые MVP+ фичи** (drag-and-drop, отчеты) → Включены во все тарифы
- **Enterprise фичи** (1С, SSO, Audit Trail) → Только Корпоративный (120,000₽/мес)
- **On-Premise** → Отдельный продукт (800,000₽)

---

## 📊 Метрики Успеха MVP+

### Цели:
- ✅ **20+ активных компаний** через 6 месяцев
- ✅ **3+ компании** на тарифе Корпоративный
- ✅ **1+ on-premise** продажа
- ✅ **Churn \u003c 5%** (удержание клиентов)
- ✅ **NPS \u003e 50** (satisfaction)

---

**Последнее обновление:** Февраль 2026  
**Версия:** 1.0

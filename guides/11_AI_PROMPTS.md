# 🤖 AI-Промпты для Разработки

> **Назначение:** Готовые промпты для AI-ассистента (Claude, ChatGPT, Cursor) для реализации каждой фичи IndustrialPROFI.

---

## 📖 Как Использовать

1. **Копируй промпт** из этого файла
2. **Вставляй в AI-ассистента** (с контекстом соответствующего документа)
3. **Проверяй результат** по чеклисту
4. **Итерируй** если нужны доработки

**Формат промптов:**
```
[КОНТЕКСТ] — какой документ прочитать
[ЗАДАЧА] — что сделать
[ТРЕБОВАНИЯ] — специфичные критерии
[ОЖИДАЕМЫЙ РЕЗУЛЬТАТ] — что должно получиться
```

---

## 🎨 Дизайн-Система и Брендинг

### ПРОМПТ 1: Настройка Tailwind с Промышленной Палитрой

```
КОНТЕКСТ:
Прочитай docs/10_BRANDING_AND_DESIGN.md раздел "Цветовая Палитра"

ЗАДАЧА:
Обнови tailwind.config.js с полной цветовой схемой IndustrialPROFI

ТРЕБОВАНИЯ:
1. Добавь все цвета из документации (primary, accent, secondary, neutral, success, warning, danger)
2. Настрой fontFamily: sans (Inter), mono (JetBrains Mono)
3. Добавь кастомные breakpoints если нужно
4. Настрой темный режим как default
5. Добавь кастомные тени для "глоу-эффекта"

ОЖИДАЕМЫЙ РЕЗУЛЬТАТ:
- tailwind.config.js полностью готов
- Все цвета доступны через className (bg-primary-900, text-accent-500, etc.)
- Темная тема работает по умолчанию
```

---

### ПРОМПТ 2: Кастомизация shadcn/ui Компонентов

```
КОНТЕКСТ:
Используем shadcn/ui, но нужно адаптировать под промышленный стиль (темная тема, красные акценты)

ЗАДАЧА:
Создай кастомные варианты Button и Card компонентов

ТРЕБОВАНИЯ:

Button variants:
- "premium": градиент красный-оранжевый, глоу-эффект при hover
- "outline": прозрачный фон, красная обводка
- "ghost": без фона, hover с легким красным подсветом

Card:
- Класс "industrial-card": темный фон #1A1A1A, тонкая border, глоу при hover
- Анимация hover: border становится красным, появляется тень

ФАЙЛЫ:
- app/frontend/components/ui/Button.tsx
- app/frontend/components/ui/Card.tsx
- app/frontend/styles/components.css (если нужны кастомные стили)

ОЖИДАЕМЫЙ РЕЗУЛЬТАТ:
- <Button variant="premium">Получить демо</Button> выглядит как в дизайне
- <Card className="industrial-card"> имеет правильный hover-эффект
```

---

## 🚀 Landing Page

### ПРОМПТ 3: Hero Section с Three.js Фоном

```
КОНТЕКСТ:
Прочитай docs/10_BRANDING_AND_DESIGN.md раздел "Three.js"

ЗАДАЧА:
Создай Hero section для landing page с 3D-фоном

ТРЕБОВАНИЯ:

Three.js фон:
1. Geometry: IcosahedronGeometry (абстрактная промышленная форма)
2. Material: wireframe, цвет #DC2626, opacity: 0.3
3. Медленная анимация вращения (rotation.x += 0.005, rotation.y += 0.005)
4. Canvas с alpha: true (прозрачный фон)
5. Responsive: на мобильных НЕ рендерить (производительность)

Контент Hero:
1. Заголовок: "Управление квалификацией промышленного персонала"
   Подзаголовок: "Избегайте штрафов за просроченные допуски. Экономьте до 200,000₽/месяц"
2. 2 кнопки: "Получить демо" (premium variant), "Посмотреть roadmap" (outline)
3. Social proof цифры: "50+ заводов", "2000+ сотрудников", "99.9% uptime"

Градиент снизу для плавного перехода к следующей секции

ФАЙЛЫ:
- app/frontend/pages/Landing/Hero.tsx
- app/frontend/components/ThreeBackground.tsx (отдельный компонент для переиспользования)

ОЖИДАЕМЫЙ РЕЗУЛЬТАТ:
- Hero section full-screen (h-screen)
- Three.js фон не тормозит (60fps)
- На мобильных показывается статичный градиент вместо Three.js
- Все тексты читаемые (высокий контраст)
```

---

### ПРОМПТ 4: GSAP Scroll Animations для Features

```
КОНТЕКСТ:
Landing page секция Features — 3-4 карточки с ключевыми фичами

ЗАДАЧА:
Добавь GSAP ScrollTrigger анимации для появления карточек при скролле

ТРЕБОВАНИЯ:

1. Карточки появляются снизу вверх (y: 100 → 0, opacity: 0 → 1)
2. Stagger: 0.2s между карточками (первая → вторая → третья)
3. Trigger: когда секция Features на 80% видимости экрана
4. Easing: 'power3.out' для плавности
5. Reverse: анимация проигрывается в обратную сторону при скролле вверх

Features карточки (4 штуки):
- Трекинг допусков (иконка ShieldCheck)
- Roadmaps профессий (иконка Map)
- Матрица навыков (иконка Users)
- Email уведомления (иконка Bell)

Каждая карточка: industrial-card стиль, иконка в красном кружке, заголовок, описание

ФАЙЛ:
- app/frontend/pages/Landing/Features.tsx

ЗАВИСИМОСТИ:
npm install gsap

ОЖИДАЕМЫЙ РЕЗУЛЬТАТ:
- Карточки плавно появляются при скролле
- Анимация не тормозит (используй will-change: transform для оптимизации)
- На мобильных анимация тоже работает
```

---

### ПРОМПТ 5: Pricing Section с Anchor Pricing

```
КОНТЕКСТ:
Прочитай docs/01_OVERVIEW.md раздел "Pricing"

ЗАДАЧА:
Создай секцию Pricing с 4 тарифами

ТРЕБОВАНИЯ:

Тарифы (слева направо):
1. СТАНДАРТ (25,000₽/мес)
2. ПРОФЕССИОНАЛ (55,000₽/мес) — RECOMMENDED badge
3. КОРПОРАТИВНЫЙ (120,000₽/мес)
4. ON-PREMISE (800,000₽ + 20,000₽/мес)

Дизайн карточки тарифа:
- industrial-card стиль
- Название тарифа (uppercase)
- Цена (крупный шрифт, моноширинный)
- Список фич (чекмарки)
- Кнопка "Выбрать" (для Профессионала — premium variant)

ПРОФЕССИОНАЛ карточка:
- Выделяется: border красный, badge "РЕКОМЕНДУЕМ" сверху
- Немного приподнята (transform: translateY(-8px))
- Более яркий глоу-эффект

ФАЙЛ:
- app/frontend/pages/Landing/Pricing.tsx

ОЖИДАЕМЫЙ РЕЗУЛЬТАТ:
- 4 карточки в grid (grid-cols-4 на десктопе, cols-1 на мобильных)
- Профессионал выделяется визуально
- Все цены читаемые (моноширинный шрифт для выравнивания)
```

---

## 📊 Dashboard и Приложение

### ПРОМПТ 6: Темная Тема для Dashboard

```
КОНТЕКСТ:
Dashboard должен выглядеть премиально — темный фон, строгая геометрия

ЗАДАЧА:
Примени промышленный стиль к основному Layout приложения

ТРЕБОВАНИЯ:

Layout структура:
- Navbar (top): черный фон (#0A0A0A), белый текст, лого слева, user menu справа
- Sidebar (left): темно-серый (#1A1A1A), навигация с иконками
- Main content: фон #0A0A0A

Navbar:
- Высота: 64px
- Лого: "INDUSTRIALPROFI" с иконкой
- Навигация: Links с hover-эффектом (красная полоска снизу)
- User menu: аватар + dropdown

Sidebar:
- Ширина: 256px (collapsible на мобильных)
- Пункты меню: иконка + текст
- Active state: красная полоска слева + красный текст
- Hover: легкий красный подсвет фона

ФАЙЛЫ:
- app/frontend/components/layout/Layout.tsx
- app/frontend/components/layout/Navbar.tsx
- app/frontend/components/layout/Sidebar.tsx

ОЖИДАЕМЫЙ РЕЗУЛЬТАТ:
- Весь Dashboard в темной теме
- Навигация интуитивная
- На мобильных Sidebar скрывается (burger menu)
```

---

### ПРОМПТ 7: Roadmap Viewer с Промышленным Стилем

```
КОНТЕКСТ:
React Flow граф roadmap должен выглядеть премиально

ЗАДАЧА:
Стилизуй React Flow под промышленный дизайн

ТРЕБОВАНИЯ:

Canvas:
- Фон: #0A0A0A (почти черный)
- Сетка (grid): очень subtle, rgba(255, 255, 255, 0.03)
- MiniMap: темный, в правом нижнем углу

Nodes (SkillNode):
- Фон зависит от статуса:
  - todo: #2A2A2A (серый)
  - in_progress: #D97706 (желтый)
  - completed: #059669 (зеленый)
  - expired: #DC2626 (красный)
- Border: 2px solid, цвет зависит от статуса
- Иконка статуса слева (Circle, Clock, CheckCircle, AlertTriangle)
- Текст: белый, читаемый
- Hover: легкий глоу-эффект

Edges (связи):
- Цвет: rgba(220, 38, 38, 0.3) (красный полупрозрачный)
- Type: smoothstep (плавные кривые)
- Animated для required зависимостей

Controls:
- Темные кнопки
- Иконки белые
- Hover с красным акцентом

ФАЙЛЫ:
- app/frontend/components/roadmap/RoadmapViewer.tsx
- app/frontend/components/roadmap/SkillNode.tsx
- app/frontend/styles/reactflow-custom.css

ОЖИДАЕМЫЙ РЕЗУЛЬТАТ:
- Граф выглядит профессионально
- Все статусы различимы визуально
- На темном фоне глаза не устают
```

---

### ПРОМПТ 8: Data Table для Матрицы Навыков

```
КОНТЕКСТ:
Manager dashboard показывает таблицу: сотрудники × навыки

ЗАДАЧА:
Создай премиальную таблицу с промышленным стилем

ТРЕБОВАНИЯ:

Дизайн:
- Моноширинный шрифт для цифр (JetBrains Mono)
- Header: фон #1A1A1A, border-bottom красный 2px, uppercase текст
- Rows: zebra striping (четные строки чуть светлее)
- Borders: очень тонкие, rgba(255, 255, 255, 0.05)
- Hover row: легкий красный подсвет

Ячейки со статусами:
- Icons вместо текста:
  - Completed: ✓ зеленый
  - In Progress: ⏳ желтый
  - Expired: ❌ красный
  - Todo: ○ серый

Фичи:
- Сортировка по столбцам (клик на header)
- Фильтр по отделу (dropdown)
- Export в CSV (кнопка сверху)

ФАЙЛ:
- app/frontend/pages/Organizations/Employees/MatrixTable.tsx

БИБЛИОТЕКА:
Используй @tanstack/react-table для функциональности

ОЖИДАЕМЫЙ РЕЗУЛЬТАТ:
- Таблица читаемая даже с 50+ сотрудниками
- Сортировка работает
- Export в CSV работает
- На мобильных таблица скроллится горизонтально
```

---

## 🎯 Специфичные Фичи

### ПРОМПТ 9: Permit Form с Валидацией

```
КОНТЕКСТ:
Сотрудник заполняет данные допуска (номер, дата выдачи, срок действия)

ЗАДАЧА:
Создай форму ввода данных допуска с валидацией

ТРЕБОВАНИЯ:

Поля формы:
1. certificate_number (текст, обязательное)
2. issued_at (дата, обязательное, не позже сегодня)
3. issuing_authority (текст, опциональное)
4. expires_at (рассчитывается автоматически на backend, показывается как read-only)

Валидация (client-side):
- certificate_number: минимум 5 символов
- issued_at: не может быть в будущем
- Все обязательные поля заполнены

UI:
- Темный фон формы
- Input поля с красным border при ошибке
- Сообщения об ошибках красным текстом под полем
- Кнопка "Сохранить" disabled пока форма невалидна

Логика:
- При submit: POST /progress/:skill_id с данными формы
- Backend рассчитывает expires_at автоматически
- Показать success toast при успешном сохранении
- Закрыть sidebar после сохранения

ФАЙЛ:
- app/frontend/components/roadmap/PermitForm.tsx

БИБЛИОТЕКА:
Используй useForm от @inertiajs/react для интеграции с Inertia

ОЖИДАЕМЫЙ РЕЗУЛЬТАТ:
- Форма интуитивно понятная
- Валидация работает
- Ошибки показываются понятно
- Submit работает без перезагрузки страницы
```

---

### ПРОМПТ 10: Dashboard Widgets (Истекающие Допуски)

```
КОНТЕКСТ:
Employee dashboard показывает виджет с предупреждением об истекающих допусках

ЗАДАЧА:
Создай alert-виджет "Истекающие допуски"

ТРЕБОВАНИЯ:

Дизайн:
- Фон: оранжевый градиент (warning)
- Border: красный, толстый (3px)
- Иконка: AlertTriangle (lucide-react)
- Заголовок: "⚠️ Истекают через 30 дней"

Контент:
- Список допусков (название + дата истечения)
- Дата в формате "01 марта 2026"
- Если допусков нет — показать зеленый виджет "Все допуски в порядке ✓"

Интерактивность:
- Клик по допуску → открывается roadmap с этим навыком
- Кнопка "Продлить" → открывает форму обновления данных

ФАЙЛ:
- app/frontend/components/dashboard/ExpiringPermitsWidget.tsx

БИБЛИОТЕКА:
- date-fns для форматирования дат

ОЖИДАЕМЫЙ РЕЗУЛЬТАТ:
- Виджет сразу привлекает внимание (оранжевый цвет)
- Понятно сколько дней осталось
- Можно быстро перейти к действию
```

---

## 🧪 Тестирование Дизайна

### ПРОМПТ 11: Lighthouse Performance Audit

```
ЗАДАЧА:
Проверь performance landing page и оптимизируй

ТРЕБОВАНИЯ:

Запусти Lighthouse audit:
1. Performance: должно быть >90
2. Accessibility: должно быть >95
3. Best Practices: должно быть 100

Проблемы которые могут быть:
- Three.js тормозит (оптимизируй: requestAnimationFrame, dispose геометрии)
- GSAP анимации не оптимизированы (добавь will-change: transform)
- Шрифты не preloaded (добавь <link rel="preload">)
- Изображения не оптимизированы (используй WebP, lazy loading)

Accessibility:
- Все кнопки имеют aria-label
- Контраст текста >4.5:1 (проверь через DevTools)
- Keyboard navigation работает

ОЖИДАЕМЫЙ РЕЗУЛЬТАТ:
- Lighthouse Performance >90
- Landing грузится <2 секунд
- Нет ошибок в консоли
```

---

## 📋 Чеклист После Каждого Промпта

После выполнения промпта проверь:

**Визуал:**
- [ ] Соответствует дизайн-системе (цвета, шрифты)
- [ ] Работает на темном фоне
- [ ] Все hover-эффекты плавные

**Код:**
- [ ] Нет ошибок в консоли
- [ ] TypeScript компилируется без errors
- [ ] Компонент переиспользуемый

**Performance:**
- [ ] Не тормозит (60fps)
- [ ] На мобильных работает

**Accessibility:**
- [ ] Контраст текста достаточный
- [ ] Можно навигировать с клавиатуры

---

## 🔄 Итерация

Если результат не идеален:

1. **Уточни промпт:** добавь больше деталей
2. **Покажи референс:** "сделай как на Linear.app"
3. **Попроси варианты:** "дай 3 варианта дизайна кнопки"

---

**Следующий шаг:** Начни с ПРОМПТ 1 (Tailwind config), потом ПРОМПТ 3 (Hero section)

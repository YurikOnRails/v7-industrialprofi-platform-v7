# Tmux Development Setup для IndustrialProfi

## Быстрый старт

Я создал два варианта tmux конфигурации для удобной разработки:

### 🎯 Вариант 1: Панели (Компактный) - `./bin/tmux-dev`
Один экран с тремя панелями:
```
┌─────────────────────────┬─────────────────────────┐
│   Rails Server          │   Vite Dev Server       │
│   (bin/rails s)         │   (bin/vite dev)        │
│                         │                         │
├─────────────────────────┴─────────────────────────┤
│   Terminal / Rails Console                        │
│   (для команд)                                    │
└───────────────────────────────────────────────────┘
```

### 🎯 Вариант 2: Отдельные окна - `./bin/tmux-dev-windows`
Пять отдельных окон:
- **0: rails** - Rails сервер
- **1: vite** - Vite dev сервер  
- **2: console** - Терминал для команд (по умолчанию)
- **3: db** - Логи PostgreSQL
- **4: git** - Git команды

## Использование

### Запуск
```bash
# Вариант с панелями
./bin/tmux-dev

# Вариант с окнами
./bin/tmux-dev-windows
```

### Основные команды tmux

**Общие:**
- `Ctrl+B`, затем `D` - отключиться (приложение продолжит работать)
- `tmux attach -t industrialprofi` - подключиться обратно
- `tmux kill-session -t industrialprofi` - завершить сессию

**Для варианта с панелями:**
- `Ctrl+B`, затем стрелки - переключение между панелями
- `Ctrl+B`, затем `O` - следующая панель
- `Ctrl+B`, затем `Z` - увеличить/уменьшить панель
- `Ctrl+B`, затем `0-9` - переключить окно

**Для варианта с окнами:**
- `Ctrl+B`, затем `0-4` - переключить окно по номеру
- `Ctrl+B`, затем `N` - следующее окно
- `Ctrl+B`, затем `P` - предыдущее окно

## Установка tmux

Если tmux не установлен:
```bash
sudo apt-get update
sudo apt-get install tmux
```

## Что запускается

1. **Rails Server** - `bin/rails s` (http://localhost:3000)
2. **Vite Dev Server** - `bin/vite dev` (для React + Inertia)
3. **Terminal** - для rails console, миграций, git команд
4. **Database Logs** - docker-compose logs (только в варианте с окнами)

## Рекомендации

- Используйте **вариант с панелями** (`./bin/tmux-dev`) если хотите видеть все логи одновременно
- Используйте **вариант с окнами** (`./bin/tmux-dev-windows`) для более чистого интерфейса и быстрого переключения

## Workflow

1. Запустите скрипт: `./bin/tmux-dev`
2. Дождитесь загрузки Rails и Vite
3. Переключитесь в панель Terminal
4. Работайте с кодом в редакторе
5. При необходимости `Ctrl+B D` для detach
6. Сессия продолжит работать в фоне

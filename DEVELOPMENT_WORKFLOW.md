# 🚀 Руководство по Эффективной Разработке: От Идеи до Первых Продаж

## 📋 Оглавление
1. [Философия: Результат > Перфекционизм](#философия)
2. [Git Workflow для Соло-разработчика](#git-workflow)
3. [Работа с ИИ-ассистентом](#работа-с-ии)
4. [Защита от Ошибок](#защита-от-ошибок)
5. [Откат Изменений](#откат-изменений)
6. [Ежедневный Workflow](#ежедневный-workflow)
7. [Чек-листы](#чек-листы)

---

## 🎯 Философия: Результат > Перфекционизм {#философия}

### Главная цель: **Первые продажи**

**Правило 80/20:**
- 80% времени - на MVP функционал
- 20% времени - на полировку

**Приоритеты:**
1. ✅ **Работающий функционал** - пусть простой, но рабочий
2. ✅ **Быстрая обратная связь** - показывайте клиентам рано
3. ✅ **Итеративное улучшение** - лучше 10 маленьких релизов, чем 1 большой
4. ❌ Идеальный код (оставьте на потом)
5. ❌ 100% покрытие тестами (начните с критичных мест)
6. ❌ Все возможные фичи сразу

**Мантра:** "Shipped is better than perfect"

---

## 🌿 Git Workflow для Соло-разработчика {#git-workflow}

### Стратегия веток: **Feature Branch + Main**

```
main (production-ready)
  ├── feature/auth
  ├── feature/products
  ├── fix/checkout-bug
  └── experiment/new-design
```

### Правила работы с ветками

#### 1. **Main ветка** - всегда рабочая
```bash
# Это ваша "production" ветка
# Здесь код должен быть стабильным
# Можно деплоить в любой момент
```

**Золотое правило:** main всегда в рабочем состоянии!

#### 2. **Feature ветки** - для новых фич

```bash
# Создание новой фичи
git checkout -b feature/user-registration

# Работаете, коммитите часто
git add .
git commit -m "Add user model"
git commit -m "Add registration form"
git commit -m "Add validation"

# Когда фича готова
git checkout main
git merge feature/user-registration --no-ff
git push origin main

# Удаляем ветку (уже не нужна)
git branch -d feature/user-registration
```

#### 3. **Fix ветки** - для багфиксов

```bash
git checkout -b fix/payment-error
# Фиксим баг
git checkout main
git merge fix/payment-error --no-ff
git branch -d fix/payment-error
```

#### 4. **Experiment ветки** - для экспериментов

```bash
git checkout -b experiment/new-payment-provider

# Эксперимент не удался? Просто удаляем
git checkout main
git branch -D experiment/new-payment-provider

# Эксперимент успешен? Мержим
git checkout main
git merge experiment/new-payment-provider --no-ff
```

### Именование веток

```bash
feature/название-фичи        # Новая функциональность
fix/описание-бага           # Исправление бага
experiment/что-пробуем      # Эксперименты (можно выбросить)
refactor/что-рефакторим     # Рефакторинг
docs/что-документируем      # Документация
```

### Частота коммитов

**Коммитьте ЧАСТО:**
```bash
# ✅ ХОРОШО - частые маленькие коммиты
git commit -m "Add User model"
git commit -m "Add password validation"
git commit -m "Add email uniqueness check"
git commit -m "Add registration form"

# ❌ ПЛОХО - один большой коммит в конце дня
git commit -m "Add user registration (5 hours of work)"
```

**Почему часто лучше:**
- Проще откатить конкретное изменение
- Легче понять что пошло не так
- ИИ лучше понимает контекст
- Можно безопасно экспериментировать

### Сообщения коммитов

```bash
# ✅ ХОРОШО - понятно что сделано
git commit -m "Add email validation to User model"
git commit -m "Fix checkout total calculation"
git commit -m "Update product card design"

# ❌ ПЛОХО - непонятно
git commit -m "fixes"
git commit -m "WIP"
git commit -m "asdf"
```

**Шаблон:**
```
<Тип>: <Краткое описание>

Типы:
- Add: новый функционал
- Fix: исправление бага
- Update: обновление существующего
- Remove: удаление
- Refactor: рефакторинг
```

---

## 🤖 Работа с ИИ-ассистентом {#работа-с-ии}

### Принципы эффективной работы

#### 1. **Начинайте с новой ветки для каждой задачи**

```bash
# Перед работой с ИИ
git checkout -b feature/product-catalog

# ИИ: "Создай каталог продуктов"
# ... работаете ...

# После проверки
git checkout main
git merge feature/product-catalog --no-ff
```

**Почему:** Всегда можно откатить ВСЁ, что ИИ натворил.

#### 2. **Коммитьте каждое значимое изменение**

```bash
# ИИ добавил модель Product
git add app/models/product.rb
git commit -m "Add Product model (AI-assisted)"

# ИИ добавил контроллер
git add app/controllers/products_controller.rb
git commit -m "Add Products controller (AI-assisted)"
```

**Почему:** Если ИИ ошибся в последнем изменении, откатите только его.

#### 3. **Проверяйте каждое изменение**

```bash
# После изменений ИИ
git diff  # Смотрим что изменилось
git status  # Какие файлы затронуты

# Запускаем приложение
./bin/tmux-dev
# Проверяем в браузере

# Если всё ок - коммитим
git add .
git commit -m "Add feature X"

# Если что-то не так - откатываем
git checkout .
```

#### 4. **Сохраняйте рабочее состояние**

```bash
# У вас всё работает? СРАЗУ коммит!
git add .
git commit -m "Working state: user can login and view products"
git push origin main

# Теперь можно экспериментировать
git checkout -b experiment/add-reviews
```

#### 5. **Используйте stash для быстрого переключения**

```bash
# Работаете над фичей, но нужно срочно фикс
git stash  # Прячем изменения

# Делаем фикс
git checkout -b fix/urgent-bug
# ... фиксим ...
git checkout main
git merge fix/urgent-bug

# Возвращаемся к фиче
git checkout feature/my-feature
git stash pop  # Достаём изменения
```

### Коммуникация с ИИ

**✅ Хорошие запросы:**
```
"Создай модель Product с полями: название, цена, описание"
"Добавь валидацию email в модель User"
"Исправь ошибку в checkout - total не считается правильно"
"Покажи как сделать связь один-ко-многим между User и Orders"
```

**❌ Плохие запросы:**
```
"Сделай интернет-магазин"  # Слишком широко
"Исправь всё"  # Непонятно что
"Добавь фичи"  # Какие?
```

**Правило:** Одна задача = один запрос = одна ветка

---

## 🛡️ Защита от Ошибок {#защита-от-ошибок}

### 1. **Git - ваша страховка**

#### Всегда коммитьте рабочее состояние
```bash
# У вас всё работает
git add .
git commit -m "✅ Working: users can register and login"
git tag checkpoint-auth-working  # Метка на всякий случай
```

#### Никогда не работайте прямо в main
```bash
# ❌ ПЛОХО
git checkout main
# делаете изменения
# что-то сломалось...

# ✅ ХОРОШО
git checkout -b feature/new-thing
# делаете изменения
# сломалось? git checkout main - и вы в безопасности
```

### 2. **Бэкапы базы данных**

```bash
# Перед большими изменениями
./bin/backup-db
```

Создадим скрипт для этого ниже.

### 3. **Чек-лист перед мержем в main**

```bash
# 1. Запустить тесты (если есть)
rails test

# 2. Запустить приложение
./bin/tmux-dev

# 3. Проверить основной функционал вручную
# - Регистрация работает?
# - Логин работает?
# - Основные страницы открываются?

# 4. Если всё ОК - мержим
git checkout main
git merge feature/my-feature --no-ff
git push origin main
```

### 4. **Environment файлы**

```bash
# ✅ В репозитории
.env.example  # Шаблон

# ❌ НЕ в репозитории (.gitignore)
.env
.env.local
.env.production
```

**Проверка:**
```bash
# Убедитесь что .env в .gitignore
grep "\.env" .gitignore
```

### 5. **Docker для изоляции**

Ваша текущая setup с docker-compose - отличная защита!

```bash
# База данных в контейнере
# Можно удалить и пересоздать в любой момент
docker-compose down -v  # Удаляет всё
docker-compose up -d    # Создаёт заново
```

---

## ⏪ Откат Изменений {#откат-изменений}

### Сценарий 1: "Только что накосячил, не коммитил"

```bash
# Откатить ВСЕ незакоммиченные изменения
git checkout .

# Откатить конкретный файл
git checkout -- app/models/user.rb

# Удалить новые неотслеживаемые файлы
git clean -fd
```

### Сценарий 2: "Накосячил и закоммитил (но не запушил)"

```bash
# Откатить последний коммит (изменения остаются)
git reset --soft HEAD~1

# Откатить последний коммит (изменения удаляются)
git reset --hard HEAD~1

# Откатить 3 последних коммита
git reset --hard HEAD~3
```

### Сценарий 3: "Накосячил, закоммитил И запушил"

```bash
# Вариант 1: Revert (создаёт новый коммит отменяющий изменения)
git revert HEAD
git push origin main

# Вариант 2: Force push (если вы один работаете)
git reset --hard HEAD~1
git push origin main --force
```

⚠️ **Force push безопасен только если вы работаете один!**

### Сценарий 4: "Всё сломалось, хочу вернуться к вчерашнему"

```bash
# Посмотреть историю
git log --oneline

# Найти хороший коммит, например: a1b2c3d
git reset --hard a1b2c3d

# Если уже запушили
git push origin main --force
```

### Сценарий 5: "Работал в main, надо было в ветке"

```bash
# Создаём ветку с текущими изменениями
git checkout -b feature/oops-forgot-branch

# Откатываем main
git checkout main
git reset --hard origin/main

# Продолжаем работу в ветке
git checkout feature/oops-forgot-branch
```

### Сценарий 6: "База данных сломалась"

```bash
# Откатить последнюю миграцию
rails db:rollback

# Откатить 3 миграции
rails db:rollback STEP=3

# Пересоздать базу с нуля
rails db:drop db:create db:migrate db:seed

# Или через Docker
docker-compose down -v
docker-compose up -d
rails db:create db:migrate db:seed
```

### Аварийный план "Всё сломалось"

```bash
# 1. Паникуем (30 секунд)
# 2. Глубокий вдох

# 3. Проверяем что изменилось
git status
git log --oneline -5
git diff HEAD~1

# 4. Откатываемся к последнему рабочему коммиту
git log --oneline --all  # Ищем коммит с "Working" или ✅
git reset --hard <хороший-коммит>

# 5. Если пушили - форс пуш
git push origin main --force

# 6. Пересоздаём базу
docker-compose down -v
docker-compose up -d
rails db:setup

# 7. Запускаем
./bin/tmux-dev

# 8. Проверяем что всё работает
# 9. Делаем чашку чая ☕
```

---

## 📅 Ежедневный Workflow {#ежедневный-workflow}

### Утро (Старт работы)

```bash
# 1. Запустить базу данных
docker-compose up -d

# 2. Проверить статус
git status
git pull origin main  # На всякий случай

# 3. Создать ветку для сегодняшней работы
git checkout -b feature/$(date +%Y-%m-%d)-user-profile

# 4. Запустить dev окружение
./bin/tmux-dev

# 5. Открыть в браузере
# http://localhost:3000
```

### В процессе работы

```bash
# Каждые 30-60 минут или после завершения подзадачи
git add .
git commit -m "Add user profile page"

# Если всё работает - пометить
git tag checkpoint-$(date +%H%M)

# Периодически пушить
git push origin feature/$(date +%Y-%m-%d)-user-profile
```

### Вечер (Конец работы)

```bash
# 1. Проверить что всё работает
# Протестировать вручную основной функционал

# 2. Если работает - мержим в main
git checkout main
git merge feature/$(date +%Y-%m-%d)-user-profile --no-ff
git push origin main

# 3. Если НЕ работает - сохраняем на завтра
git push origin feature/$(date +%Y-%m-%d)-user-profile

# 4. Останавливаем (или оставляем в фоне)
tmux detach  # Ctrl+B, затем D

# 5. Дневной отчёт (опционально)
git log --oneline --since="1 day ago"
```

### Еженедельный ритуал (пятница)

```bash
# 1. Убедиться что main в порядке
git checkout main
git pull

# 2. Запустить всё и протестировать
./bin/tmux-dev

# 3. Создать backup базы
./bin/backup-db

# 4. Почистить старые ветки
git branch --merged | grep -v "main" | xargs git branch -d

# 5. Создать release tag
git tag release-$(date +%Y-%m-%d)
git push origin release-$(date +%Y-%m-%d)

# 6. Записать что сделано за неделю
# Какие фичи готовы?
# Что осталось до MVP?
# Сколько до первых продаж?
```

---

## ✅ Чек-листы {#чек-листы}

### Перед началом новой фичи

- [ ] Main ветка в рабочем состоянии?
- [ ] Создана новая feature ветка?
- [ ] Запущена база данных?
- [ ] Запущен dev сервер (tmux)?
- [ ] Понятна задача?

### Перед коммитом

- [ ] Проверили изменения (`git diff`)?
- [ ] Приложение запускается?
- [ ] Основной функционал работает?
- [ ] Нет ли конфиденциальных данных (.env, пароли)?
- [ ] Осмысленное сообщение коммита?

### Перед мержем в main

- [ ] Все тесты проходят (если есть)?
- [ ] Приложение запускается без ошибок?
- [ ] Протестировали вручную?
- [ ] Нет лишних console.log / debugger?
- [ ] Миграции применились корректно?

### Перед деплоем (когда дойдёте)

- [ ] Main стабильный?
- [ ] Создан backup базы?
- [ ] Environment переменные настроены?
- [ ] Протестировано на production-like окружении?
- [ ] Есть план отката?

---

## 🎯 Фокус на результат: Дорожная карта до первых продаж

### Фаза 1: MVP (Минимально Жизнеспособный Продукт) - 2-4 недели

**Цель:** Что-то что можно показать клиенту

**Обязательно:**
- [ ] Регистрация/Логин
- [ ] Главная страница
- [ ] 1-2 ключевые фичи
- [ ] Простой дизайн (Tailwind хватит)

**Не нужно:**
- ❌ Идеальный дизайн
- ❌ Все фичи
- ❌ Оптимизация
- ❌ 100% тесты

**Git стратегия:**
```bash
feature/auth
feature/main-page
feature/core-feature-1
feature/core-feature-2
```

### Фаза 2: Альфа-версия - 1-2 недели

**Цель:** Дать 3-5 людям потестировать

**Добавляем:**
- [ ] Обработка ошибок
- [ ] Валидация форм
- [ ] Базовая безопасность
- [ ] Deploy на сервер

**Git стратегия:**
```bash
fix/error-handling
feature/form-validation
feature/deploy
```

### Фаза 3: Бета - 2-3 недели

**Цель:** Реальные пользователи

**Добавляем:**
- [ ] Аналитика
- [ ] Feedback форма
- [ ] Доработки по отзывам
- [ ] Платёжная система (если нужна)

### Фаза 4: Первые продажи! 🎉

**Продолжаем итерировать:**
- Слушаем пользователей
- Фиксим баги
- Добавляем фичи по приоритету

---

## 🛠️ Полезные команды и алиасы

Создадим файл с алиасами для упрощения работы:

```bash
# См. файл .git-aliases ниже
```

---

## 📝 Заметки и советы

### Работа с ИИ

1. **Разбивайте большие задачи**
   - Вместо: "Создай интернет-магазин"
   - Лучше: "Создай модель Product", затем "Создай каталог продуктов"

2. **Проверяйте каждое изменение**
   - ИИ может ошибаться
   - Всегда тестируйте в браузере
   - Коммитьте только проверенное

3. **Сохраняйте контекст**
   - Работайте в одной ветке над одной фичей
   - Не прыгайте между задачами

### Защита от выгорания

1. **Коммитьте часто** - маленькие победы мотивируют
2. **Отмечайте прогресс** - ведите changelog
3. **Делайте перерывы** - каждые 2 часа
4. **Один день в неделю** - без кода, только планирование

### Продуктивность

1. **Timebox** - 2 часа на фичу, не получилось - упростите
2. **Done > Perfect** - лучше работающее плохо, чем не работающее хорошо
3. **Удаляйте код** - не работает? Удалить и начать заново
4. **Спрашивайте ИИ** - застряли 30 минут? Спросите ИИ

---

## 🚨 Красные флаги

**Остановитесь и переосмыслите если:**

- ❌ Работаете над одной фичей больше недели
- ❌ Не можете объяснить что делает ваш код
- ❌ Боитесь что-то менять (значит нет защиты через Git)
- ❌ Не коммитили больше 2 часов
- ❌ Main сломан больше дня
- ❌ Не знаете как откатить изменения

**Решение:** Откат → Упрощение → Маленькие шаги

---

## 🎓 Правила которым надо следовать

### Железные правила

1. **Main всегда рабочий** - можно задеплоить в любой момент
2. **Коммитить каждый час** - маленькие атомарные коммиты
3. **Новая фича = новая ветка** - всегда
4. **Работает? Коммит!** - зафиксируйте рабочее состояние
5. **Один день = один PR/merge** - пусть маленький, но прогресс

### Запреты

1. **НЕ работайте в main напрямую** - только через ветки
2. **НЕ коммитьте .env файлы** - только .env.example
3. **НЕ коммитьте закомментированный код** - удалите (Git помнит)
4. **НЕ копите изменения** - коммитьте часто
5. **НЕ бойтесь удалять** - Git помнит всё

---

## 📦 Что дальше?

1. Прочитать это руководство ✅
2. Настроить алиасы (см. bin/git-setup)
3. Создать первую feature ветку
4. Начать с малого - одна фича за раз
5. Коммитить часто
6. Двигаться к MVP

**Помните:** Лучший код - это запущенный код!

---

*Последнее обновление: февраль 2026*
*Версия: 1.0*
*Автор: Для команды IndustrialProfi*

# Quick Reference для Daily Development

## 🚀 Быстрый старт день

```bash
# 1. База данных
docker-compose up -d

# 2. Dev окружение
./bin/tmux-dev

# 3. Новая фича
git checkout -b feature/my-feature
```

## 📝 Частые команды

### Git Workflow

```bash
# Создать feature ветку
git checkout -b feature/название

# Частые коммиты
git add .
git commit -m "Add user model"

# Проверить что изменилось
git status
git diff

# Смержить в main (когда готово)
git checkout main
git merge feature/название --no-ff
git push origin main

# Удалить ветку
git branch -d feature/название
```

### Откат изменений

```bash
# Не коммитил - откат всего
git checkout .

# Закоммитил - откат последнего
git reset --soft HEAD~1   # Изменения остаются
git reset --hard HEAD~1   # Изменения удаляются

# Запушил - revert
git revert HEAD
git push origin main
```

### База данных

```bash
# Backup
./bin/backup-db

# Restore
./bin/restore-db backups/db/файл.sql

# Миграции
rails db:migrate          # Применить
rails db:rollback         # Откатить последнюю
rails db:rollback STEP=3  # Откатить 3

# Пересоздать
rails db:drop db:create db:migrate db:seed
```

## ✅ Чек-лист перед коммитом

- [ ] `git diff` - проверил изменения?
- [ ] Приложение запускается?
- [ ] Основной функционал работает?
- [ ] Нет .env файлов?
- [ ] Осмысленное сообщение коммита?

## 🆘 SOS - Всё сломалось

```bash
# 1. Паника (30 сек)
# 2. Глубокий вдох

# 3. Откат к последнему рабочему
git log --oneline -10
git reset --hard <хороший-коммит>

# 4. Если запушили
git push origin main --force

# 5. База данных
docker-compose down -v
docker-compose up -d
rails db:setup

# 6. Запуск
./bin/tmux-dev
```

## 🎯 Принципы

1. **Коммитьте часто** - каждые 30-60 минут
2. **Main всегда рабочий** - через ветки
3. **Работает? Коммит!** - зафиксируйте
4. **Не бойтесь удалять** - Git помнит
5. **Done > Perfect** - результат важнее

## 🔧 Настройка (один раз)

```bash
./bin/git-setup  # Установить алиасы и хуки
```

После этого доступно:

```bash
git visual              # Красивая история
git new-feature <name>  # Создать feature ветку
git today              # Что сделано сегодня
git undo               # Откатить последний коммит
git cleanup            # Удалить старые ветки
```

---

📖 **Полная документация:** DEVELOPMENT_WORKFLOW.md
